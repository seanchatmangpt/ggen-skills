# ggen-skills bootstrap fence

This root commit exists only because an empty Git repository has no SHA from which a purpose branch can be created.

The canonical implementation is developed through a purpose branch from this exact bootstrap commit.

## Non-negotiable boundary

- Canonical truth is semantic source plus GGen manufacturing configuration.
- Skills, agent instructions, runtime projections, registries, and generated documentation are not hand-authored editing surfaces.
- `ggen sync run` is the only writer for those projections.
- `SELECT`, `CONSTRUCT`, and `DO` are distinct. Generated instructions and model output have no ambient actuation authority.
- Core-team standing uses only `UNKNOWN`, `PARTIAL_ALIVE`, `ALIVE`, `BLOCKED`, `BUILD_BROKEN`, `UNSUPPORTED`, and typed `REFUSED`.
- `ALIVE` requires observed execution against the exact admitted subject, receipt verification, deterministic replay, and exact-subject binding.
- DfCM order is Preserve -> Fence -> Calculus -> Exclusions -> Falsifier -> Extension -> Operationalization.

No application capability is claimed by this bootstrap commit.
