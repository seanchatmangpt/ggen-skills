# Example: authoring a new domain skill

```text
skill-creator-dfcm
-> skill-composer
-> skill-falsifier
-> skill-qualifier
-> skill-promoter
```

The creator should inspect existing skills first, preserve trigger semantics that already work, enumerate packaging alternatives, and select boundaries based on reuse, authority separation, and evidence—not minimum file count.
