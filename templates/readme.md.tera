# {{ results[0].title }}

> GENERATED by `ggen sync run` from the admitted semantic source. DO NOT EDIT BY HAND.

{{ results[0].body }}

## Bootstrap capability set

The first closed field manufactures three meta-skills across Codex, Claude, and Gemini projections:

- `meta-router`
- `skill-admission`
- `evidence-optimizer`

That is 3 canonical skills × 3 runtime projections = 9 generated `SKILL.md` artifacts from one ontology.

## Operator surface

- `just doctor` — source admission and ownership checks.
- `just chicago` — real GGen manufacture, receipt verification, deterministic second sync, and generated-consequence verification.
- `just falsify` — negative fixtures proving the verifier rejects forbidden states.
- `just dod` — complete local core-team Definition of Done.

The GitHub verifier is read-only evidence transport. It does not push, merge, deploy, or otherwise acquire `DO` authority.
